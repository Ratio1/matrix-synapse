# Matrix homeserver configuration
MATRIX_SERVER_NAME=matrix.local
MATRIX_PORT=8008

# Ratio1 Worker App settings
DATA_DIR=/data
CONFIG_FILE=/data/homeserver.yaml

# Optional: Database settings
SYNAPSE_REPORT_STATS=no

# Optional: Resource limits for Worker App Runner
WORKER_CPU_LIMIT=1
WORKER_MEMORY_LIMIT=2GB
WORKER_STORAGE_LIMIT=5GB
